<mvc:View
  controllerName="sapui.demo.walkthrough.js.controller.InvoiceList"
  xmlns="sap.m"
  xmlns:mvc="sap.ui.core.mvc">
  <List
    headerText="{i18n>invoiceListTitle}"
    class="sapUiResponsiveMargin"
    width="auto"
    items="{invoice>/Invoices}">
    <items>
      <ObjectListItem
        title="{invoice>Quantity} x {invoice>ProductName}"
        number="{
          parts: [{path: 'invoice>ExtendedPrice'}, {path: 'view>/currency'}],
          type: 'sap.ui.model.type.Currency',
          formatOptions: { 
            showMeasure: false,
            decimalSeparator: '.',
            groupingSeparator: ','
          }
        }"
        icon="{= ${invoice>ExtendedPrice} > ${.formatter.threshold.max} ? 'sap-icon://warning2' : '' }"
        numberUnit="{view>/currency}"
        numberState="{
          parts: [
            {path: 'invoice>ExtendedPrice'}
          ],
          formatter: '.formatter.getState'
        }">
        <firstStatus>
          <ObjectStatus
            text="{path: 'invoice>Status', formatter: '.formatter.statusText'}"/>
        </firstStatus>
      </ObjectListItem>
    </items>
  </List>
</mvc:View>